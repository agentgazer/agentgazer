services:
  agentgazer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18800:18800"
      - "18900:18900"
    volumes:
      - agentgazer-data:/root/.agentgazer
    # environment:
    #   AGENTGAZER_SECRET_BACKEND: machine  # default; uses encrypted file with machine-derived key

volumes:
  agentgazer-data:
